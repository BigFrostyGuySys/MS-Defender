DeviceFileEvents
| where ActionType == "FileCreated"
| extend FP =tolower(FolderPath)
| where FP contains "appdata"
| extend FN = tolower(FileName)
| where FN contains ".crx"
| project Timestamp, DeviceName, FileName, FolderPath, InitiatingProcessFileName, InitiatingProcessCommandLine
